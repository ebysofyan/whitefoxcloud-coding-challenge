# DynamoDB Local endpoint (set for local dev, unset for production AWS)
DYNAMODB_ENDPOINT=http://localhost:18749

# AWS region
AWS_REGION=ap-southeast-1

# Runtime environment label (dev, staging, prod)
# Table names are auto-prefixed: {ENVIRONMENT}-books
ENVIRONMENT=dev
